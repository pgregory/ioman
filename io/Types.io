RtPoint := Vector clone setSize(3)
RtColor := Vector clone setSize(3)
RtMatrix := Vector clone setSize(16)
RtBasis := RtMatrix clone
RtBound := Vector clone setSize(6)
RtString := Sequence clone
RtToken := Sequence clone
